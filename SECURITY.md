# Security Policy

[English](SECURITY.en.md) | 简体中文

## 支持版本

目前仅支持默认分支上的最新版本。实验性版本不承诺生产环境的安全性或稳定性。

## 报告安全问题

请不要公开提交包含漏洞细节、凭据、完整请求、模板、日志或 KV 快照的 Issue。请通过 GitHub Security Advisory 或维护者配置的私密联系方式报告，并提供：

- 受影响的版本和提交；
- 复现步骤或最小示例；
- 影响范围；
- 建议的修复方向（如有）。

维护者会确认收到报告，并在修复后发布公告或变更说明。请给维护者合理的修复时间，不要在修复前公开披露。

## 部署安全边界

- 默认只监听回环地址；
- 本项目没有认证、TLS、限流或多租户隔离；
- 不要把管理 API 直接暴露到互联网；
- 将 KV 快照和模板视为敏感数据，按模型输入和组织策略保护；
- 不要让不可信客户端绕过中间件访问同一 llama.cpp slot。
